import { DefaultTypePipePipe } from './default-type-pipe.pipe';

describe('DefaultTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
